---

- name: Desktop and laptop playbook
  hosts: localhost
  connection: local
  become: true
  
  vars_files:
    - vars/secret.yml

  tasks:
    - name: echo email
      shell: echo "{{ email }}"   
    - include_tasks: tasks/odin/main.yml
      when: "'Odin' in ansible_hostname" 
